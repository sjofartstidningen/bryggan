/**
 * This file defines the custom scalars used on the GraphQL endpoint. The base
 * types itself are automatically generated when running `yarn generate:types`
 * or `yarn generate:types:watch`.
 *
 * But the codegen-script will pass through any custom scalar and prefix them
 * with `GraphQL_`.
 */

type GraphQL_PathLike = string;
